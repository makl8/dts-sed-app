---
name: Continuous Integration
run-name: ${{ github.workflow }} - ${{ github.actor }} pushed a change to ${{ github.ref_name }}

on:  # yamllint disable-line rule:truthy
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  pytest:
    name: pytest
    if: github.head_ref != 'release-please--branches--main'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.14"]

    steps:
      - name: Check out the repository to the runner
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: |-
          python -m pip install --upgrade pip
          if [ -f pyproject.toml ]; then pip install ".[test]"; fi

      - name: Run tests with pytest
        env:
          DJANGO_SETTINGS_MODULE: learning.settings
          SECRET_KEY: "pytest-only-not-used-at-runtime"
        run: |
          if [ -d tests ]; then pytest tests --cov -vv ; fi

  megalinter:
    name: MegaLinter
    if: github.head_ref != 'release-please--branches--main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: false

      - name: MegaLinter
        uses: oxsecurity/megalinter/flavors/python@v10
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DJANGO_SETTINGS_MODULE: learning.settings
          SECRET_KEY: "pylint-only-not-used-at-runtime"
        with:
          flake8_config_path: .flake8
          ruff_config_path: pyproject.toml
